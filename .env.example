FLASK_DEBUG=true
# These must match your PostgreSQL server (user, password, database name).
DATABASE_NAME=hackathon_db
DATABASE_HOST=localhost
# Must match docker-compose.yml host port (container listens on 5432 internally).
DATABASE_PORT=15432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
