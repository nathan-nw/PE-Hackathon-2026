apiVersion: v1
kind: ConfigMap
metadata:
  name: url-shortener-config
  namespace: pe-hackathon
data:
  DATABASE_NAME: hackathon_db
  DATABASE_PORT: "5432"
  DATABASE_USER: postgres
  FLASK_DEBUG: "false"
  LOG_LEVEL: INFO
  # In-cluster Postgres Service (see postgres-service.yaml); override for managed DB.
  DATABASE_HOST: db
