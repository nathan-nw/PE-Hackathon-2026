apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: pe-hackathon
type: Opaque
stringData:
  # Dev default — replace in production (Sealed Secrets / external vault).
  password: postgres
